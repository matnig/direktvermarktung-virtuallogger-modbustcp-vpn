node_modules/

# Runtime data files are auto-created on first start.
# ALLES unterhalb von data/ ignorieren (nicht nur *.json): der Verlaufs-Logger legt
# dort Unterverzeichnisse mit .ndjson an, die sonst im Repo landen — passiert am
# 2026-09-06 genau so und hat lokale Testdaten auf die Anlage geschleppt.
src/persistence/data/**
!src/persistence/data/.gitkeep

# Environment overrides
.env

# OS
.DS_Store

.claude/

# Fremde Hersteller-/Netzbetreiber-Dokumente (nicht ins öffentliche Repo)
*.pdf
